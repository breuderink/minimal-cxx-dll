This repo contains a quick and dirty example of building dynamic link libraries
(coupled at compile time) on Windows (through mingw32), Linux and OSX. The
magic is in the Makefile.

The examples are based on the example in the StackOverflow post
http://stackoverflow.com/a/497067#.
